# AGENTS.md — Character Room Builder

## 작업 전 정본

1. 제품 정의는 docs/PRODUCT.md를 읽는다.
2. 신뢰 경계와 계층은 docs/ARCHITECTURE.md를 읽는다.
3. 데이터 형식은 docs/DATA-MODEL.md와 lib/room/types.ts를 확인한다.
4. UI 작업은 STYLESEED.md를 먼저 읽는다.
5. AI 도움 공개는 docs/AI-ASSISTANCE.md를 따른다.

## 불변 원칙

- 생성형 이미지 모델을 제품 에셋 파이프라인에 사용하지 않는다.
- 방 데이터, 렌더러, 외부 데이터 어댑터를 분리한다.
- 동일한 데이터와 렌더러 버전은 동일한 방을 만든다.
- 로그인과 네트워크가 없는 로컬 모드가 기본이다.
- Google Sheets는 선택적 어댑터이며 렌더러가 이를 알지 않는다.
- 외부 코드·폰트·에셋의 출처와 라이선스를 기록한다.
- Codex와 Claude Code의 도움을 사이트와 저장소에서 공개한다.

## UI와 렌더러

- 제품 셸과 방 결과물의 시각 언어를 구분한다.
- STYLESEED.md 밖의 액센트·라디우스·그림자를 임의로 추가하지 않는다.
- 방 그래픽은 Canvas 코드, 정식 라이선스 팩, 사용자 제공 에셋만 사용한다.
- 오브젝트를 그리는 코드가 RoomDocument를 무시하는 고정 장면이 되지 않게 한다.
- 장식용 이모지와 반복 아이콘 칩을 사용하지 않는다.
- UI 완료를 주장하기 전에 실제 렌더링을 본다.

## 검증

- 변경에 맞는 빌드와 코드 검사를 실행한다.
- 방 데이터 변경은 결정론과 직렬화 가능성을 확인한다.
- 공개 전 개인정보·비밀정보·로컬 절대 경로를 검색한다.
- 스타일 팩 메타데이터에 버전, 구현자, 라이선스, 생성형 이미지 사용 여부를 둔다.

## Windows 한국어 안전

한국어 파일은 UTF-8로 읽고 쓰며 인코딩을 명시한다. PowerShell 기본 리디렉션, 기본 Out-File, cmd echo로 한국어 파일을 수정하지 않는다. 깨진 파일은 추측으로 덮어쓰지 않는다.
