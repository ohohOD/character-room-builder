import{spawn}from"node:child_process";import process from"node:process";const command=process.argv[2];if(!["dev","build","start"].includes(command))throw new Error("Expected dev, build, or start");const executable=process.platform==="win32"?"vinext.cmd":"vinext";const child=spawn(executable,[command],{env:{...process.env,WRANGLER_LOG_PATH:".wrangler/wrangler.log"},shell:process.platform==="win32",stdio:"inherit"});child.on("exit",code=>process.exit(code??1));
